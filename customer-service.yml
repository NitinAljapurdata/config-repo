spring:
    config:
      import: optional:configserver:http://localhost:8888
    datasource:
      username: root
      password: 246052
      url: jdbc:mysql://localhost:3306/customer_master?useSSL=false&serverTimezone=UTC
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
